//
// ININIntegrationStatusInfo.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** Status information for an Integration. */
public class ININIntegrationStatusInfo: JSONEncodable {
    public enum ININInternal: String { 
        case Active = "ACTIVE"
        case Activating = "ACTIVATING"
        case Inactive = "INACTIVE"
        case Deactivating = "DEACTIVATING"
        case Error = "ERROR"
    }
    /** Machine-readable status as reported by the integration. */
    public var _internal: ININInternal?
    /** Localized, human-readable, effective status of the integration. */
    public var effective: String?
    /** Localized status details for the integration. */
    public var detail: String?
    /** Date and time (in UTC) when the integration status (i.e. the internal field) was last updated. Date time is represented as an ISO-8601 string. For example: yyyy-MM-ddTHH:mm:ss.SSSZ */
    public var lastUpdated: NSDate?

    public init() {}

    // MARK: JSONEncodable
    func encodeToJSON() -> AnyObject {
        var nillableDictionary = [String:AnyObject?]()
        nillableDictionary["internal"] = self._internal?.rawValue
        nillableDictionary["effective"] = self.effective
        nillableDictionary["detail"] = self.detail
        nillableDictionary["lastUpdated"] = self.lastUpdated?.encodeToJSON()
        let dictionary: [String:AnyObject] = APIHelper.rejectNil(nillableDictionary) ?? [:]
        return dictionary
    }
}
